---
description: "[ZH mirror] Python — 不自动注入"
globs: []
alwaysApply: false
---

## 基础
- 全量类型注解，函数入参和返回值都要标
- 用 mypy 检查，不能有 ignore 注释除非有解释
- f-string 优先，不用 % 格式化，不用 .format()

## 包管理
- 用 stack.md 中指定的 Python 包管理工具
- 依赖锁文件提交到 git
- 不用 requirements.txt（除非部署平台强制要求）

## 脚本
- 使用 stack.md 中指定的 CLI 框架
- 参数验证交给框架，不在函数体里手写 if
- 错误输出到 stderr + 非零退出码
- 业务逻辑抽成纯函数，和 CLI 入口分离

## 服务（FastAPI）
- Router 按领域拆分，挂载到 main app
- 依赖注入用 Depends()，不用全局变量
- Response model 显式声明，不返回裸 dict
- 异步函数用 async def，同步 IO 操作用 run_in_executor

## 代码风格
- 函数超 40 行拆分
- 不用可变默认参数（def f(x=[]) 是 bug）
- dataclass 或 Pydantic model 代替裸 dict 传数据
