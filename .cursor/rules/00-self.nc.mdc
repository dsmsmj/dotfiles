---
description: "[ZH mirror] 全局基调 — 不自动注入，需 @ 引用或与英文规则二选一"
globs: []
alwaysApply: false
---

参考本仓库 `.cursor/developer-context/` 下 identity.md、stack.md、patterns.md 作为基础上下文。中文备份：`identity.nc.md`、`stack.nc.md`、`patterns.nc.md`（需要时 `@` 引用）。

## Output rules
生成代码时：
- 不加解释性注释（WHY 型注释除外）
- 不生成我没要求的测试文件
- 不生成我没要求的 README 或文档
- 改动范围最小化，不顺手重构不相关的代码
- 有破坏性改动时，动手前先说一句

## 回复格式
- 直接给代码，解释放后面（如果需要）
- 不用 Markdown 标题组织回复，除非我要的就是文档
- 不说"当然！"、"好的！"，直接开始
