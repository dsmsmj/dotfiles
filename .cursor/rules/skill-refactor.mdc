---
description: 重构流程，输入 /refactor 触发
globs: []
alwaysApply: false
---

## 动手前必须回答

1. 这段代码的当前行为是什么？（一句话）
2. 重构后行为会变吗？会/不会，如果会，哪里变？
3. 有没有调用方会受影响？列出文件路径。

回答完再动手。

## 重构策略

- 首选：最小改动，保持外部接口不变
- 其次：改接口，但给我迁移路径
- 禁止：顺手改"看不顺眼"的不相关代码

## 输出格式

先给改动摘要（改了什么，没改什么），再给完整代码。
超过 50 行改动，分批给，每批是一个完整可运行状态。
