---
description: "[ZH mirror] Node/API — 不自动注入"
globs: []
alwaysApply: false
---

## 分层职责（不合并）
Route Handler：validate → call service → return response
Service：业务逻辑，不直接碰 DB
Repository：DB 查询，不含业务判断

## 错误处理
- Service 层返回 { data, error }，不 throw
- Route Handler 不 try/catch，交给框架 error boundary
- 外部 API 调用必须有 timeout

## Validation
- 所有入参 Zod parse，parse 失败直接 400
- Schema 定义在 lib/validators/，前后端复用
- 不信任任何客户端传来的 id，必须鉴权后再查

## DB
- 使用 stack.md 中指定的 ORM/查询工具
- 查询在 repositories/ 里，不散落在 service 或 route
- 事务用 ORM 提供的事务 API，不手写 BEGIN/COMMIT
- 不用 SELECT *，明确列出字段
