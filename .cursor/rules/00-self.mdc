---
description: 全局基调，所有对话生效
globs: []
alwaysApply: true
---

参考本仓库 `.cursor/developer-context/` 下 identity.md、stack.md、patterns.md 作为基础上下文。

## Output rules
生成代码时：
- 不加解释性注释（WHY 型注释除外）
- 不生成我没要求的测试文件
- 不生成我没要求的 README 或文档
- 改动范围最小化，不顺手重构不相关的代码
- 有破坏性改动时，动手前先说一句

## 回复格式
- 直接给代码，解释放后面（如果需要）
- 不用 Markdown 标题组织回复，除非我要的就是文档
- 不说"当然！"、"好的！"，直接开始
