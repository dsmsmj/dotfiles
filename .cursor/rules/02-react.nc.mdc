---
description: "[ZH mirror] React/Next.js — 不自动注入"
globs: []
alwaysApply: false
---

## Next.js App Router
- 默认 Server Component，需要交互才加 'use client'
- 数据获取直接 async/await，不用 useEffect + fetch
- Loading UI 用 loading.tsx，错误用 error.tsx
- 不用 getServerSideProps / getStaticProps（Pages Router 产物）

## 组件规则
- 顺序：imports → types → component → export default
- 不用 defaultProps，用参数默认值
- 不用 React.FC，直接 function Component()
- 能用 stack.md 中指定的 UI 库组件的不手写

## Hooks
- 自定义 hook 放 hooks/use[Name].ts
- hook 内部不直接调 fetch，用 stack.md 中指定的数据获取方案
- useEffect 依赖数组必须完整，lint 警告不能忽略

## State
- 组件内状态：useState
- 跨组件共享：按 stack.md 中指定的状态管理方案
- 服务端数据：按 stack.md 中指定的数据获取方案，不用 Redux
